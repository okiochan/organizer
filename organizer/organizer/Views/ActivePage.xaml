<Page x:Class="organizer.ActivePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:organizer"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="ActivePage" Name="activePage" Loaded="activePage_Loaded">

    <!-- right col -->
    <StackPanel  Margin="0 0 10 0" Grid.ColumnSpan="2">

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
            <TextBlock  HorizontalAlignment="Center" Text="Active tasks list" Background="AliceBlue" FontWeight="Bold"  FontSize="15" x:Name="txtBlockTasksList"/>
            <Button Margin="10 10 " x:Name="butAddTask" Click="butAddTask_Click" Background="LightYellow" FontWeight="Bold">__add task__</Button>
        </StackPanel>

        <!-- add here tasks -->

        <ScrollViewer   Height="400" >
            <StackPanel Orientation="Vertical" x:Name="panelTasks"   >
            </StackPanel>
        </ScrollViewer>

    </StackPanel>


</Page>
